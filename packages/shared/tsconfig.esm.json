{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "ES2022",
    "moduleResolution": "Bundler",
    "outDir": "dist/esm",
    // Declarations come from the CommonJS pass; emitting them twice would just duplicate.
    "declaration": false
  }
}
