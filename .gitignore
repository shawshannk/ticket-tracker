# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
.turbo/
apps/**/dist/
packages/**/dist/

# Environment
.env
.env.local
.env.*.local
apps/api/.env

# Database
*.sqlite

# Testing
coverage/
playwright-report/
test-results/
.playwright/

# Agent tooling
# A mirror of the tracked project skill in .claude/skills/, written by the harness when a skill
# is invoked. Ignored rather than committed so the same skill does not live in git twice —
# .claude/skills/ stays the source of truth.
.agents/

# Editor / OS
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# Drizzle
drizzle/meta/_journal.json.bak

# Playwright
test-results/
playwright-report/
